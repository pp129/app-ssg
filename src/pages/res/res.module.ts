import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ResPage } from './res';

@NgModule({
  declarations: [
    ResPage,
  ],
  imports: [
    IonicPageModule.forChild(ResPage),
  ],
})
export class ResPageModule {}
